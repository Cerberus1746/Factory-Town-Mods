using Harmony;
using System;

namespace ModdingAPI {
    /*[HarmonyPatch(typeof(Enum))]
    [HarmonyPatch("IsDefined")]
    [HarmonyPatch(new Type[] { typeof(Type), typeof(object) })]
    static class IsDefined_Patch {
        public static bool Prefix(Type enumType, object value, ref bool __result) {
            //
        }
    }


    [HarmonyPatch(typeof(Enum))]
    [HarmonyPatch("Parse")]
    [HarmonyPatch(new Type[] { typeof(Type), typeof(string) })]
    static class Parse_Patch {
        public static bool Prefix(Type enumType, string value, ref object __result) {
            //
        }
    }*/
}
